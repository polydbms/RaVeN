
experiments:
  controller:
    results_folder: ~/git/master-raster-vector/data/results
    results_db: ~/git/master-raster-vector/data/results.db
  hosts:
    - host: "sgx10.dima.tub"
      base_path: /local-ssd/gdusella-ldap
      public_key_path: ~/.ssh/lrv.pub
  data:
    raster: /home/gereon/git/master-raster-vector/benchi/data/glc/GlcShare_v10_02
    vector: /home/gereon/git/master-raster-vector/benchi/data/us_state
  workload:
    get: 
      vector:
        - AFFGEOID
      raster: 
        - sval:
            aggregations:
              - avg
              - max
              - min
              - count
              - sum
    join:
      table1: vector
      table2: raster
      condition: intersect(raster, vector)
    group:
      vector: 
        - AFFGEOID
    order:
      vector: 
        - AFFGEOID

  systems: 
    - name: postgis
      port: 25432
    - name: beast

